# Frontend

The frontend is intentionally deferred.

We will add the frontend in its own phase after the backend has enough useful
API behavior to display. For now, do not run `npm install` here and do not add a
Vite/React scaffold.
